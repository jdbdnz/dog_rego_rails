<tr class="<%= "active" if registration.current? %>" >
  <td><%= registration.id %></td>
  <td><%= format_date registration.valid_from %></td>
  <td><%= format_date registration.valid_until %></td>
  <td><%= number_to_currency registration.fee %></td>
  <td class="text-center">
    <% if registration.paid? %>
      <span class="glyphicon glyphicon-ok text-success" aria-hidden="true"></span>
    <% else %>
      <% if policy(:registration).paid? %>
        <%= link_to "Mark Paid", user_dog_registration_paid_path(registration.owner, registration.dog, registration), method: :put, class: 'btn btn-success' %>
      <% else %>
        <span class="glyphicon glyphicon-remove text-danger" aria-hidden="true"></span>
      <% end %>
    <% end %>
  </td>
</tr>