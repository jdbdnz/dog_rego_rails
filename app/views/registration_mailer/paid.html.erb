<%= render 'mailer_header' %>
<p>This email is to confirm we've received payment for <%= @dog.name %>'s registration between <%= "#{ @registration.valid_from.strftime('%e %b %Y') } and #{ @registration.valid_until.strftime('%e %b %Y') }" %>.
<%= render 'mailer_footer' %>